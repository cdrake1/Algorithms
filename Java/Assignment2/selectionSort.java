package Assignment2;

public class selectionSort {
    public void sort(String[] textArray) {
        
        for (int i = 0; i < textArray.length; i++) { //read through each line of array to sort
            String min = textArray[i]; //set minimum to current index
            if()// compare strings
        }
    }
}
