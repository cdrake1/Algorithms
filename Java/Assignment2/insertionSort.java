package Assignment2;

public class insertionSort {
    public String[] sort(String[] textArray){
        for(int i = 1; i < textArray.length; i++){ //iterate through array
            String marker = textArray[i]; //set marker to index
            int j = i - 1;
            
            while(){
                
            }

        }
        return textArray;
    }
    
}
